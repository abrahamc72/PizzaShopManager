import { createTheme } from '@mui/material/styles';
const theme = createTheme({
  palette: {
    primary: {
      main: "#840f0f",
    },
    scrollbar: {
      thumbColor: "#ffffff",
      trackColor: "#222222",
    },
  },
});
export default theme;